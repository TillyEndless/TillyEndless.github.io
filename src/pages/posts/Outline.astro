---
const { headings = [] } = Astro.props; // 如果 headings 未定义，默认为空数组
---

<nav>
  <h2>大纲</h2>
  <ul>
    {headings.length > 0 ? (
      headings.map((heading) => (
        <li key={heading.id} style={{ marginLeft: `${(heading.level - 1) * 1.5}rem` }}>
          <a href={`#${heading.id}`}>{heading.text}</a>
        </li>
      ))
    ) : (
      <li>没有大纲内容</li> // 当 headings 为空时显示的消息
    )}
  </ul>
</nav>